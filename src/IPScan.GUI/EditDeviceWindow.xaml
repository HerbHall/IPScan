<Window x:Class="IPScan.GUI.EditDeviceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Edit Device"
        Height="400" Width="450"
        MinHeight="350" MinWidth="400"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}"
        ShowInTaskbar="False"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Edit Device" FontSize="20" FontWeight="SemiBold"
                       Foreground="{DynamicResource TitleBrush}"/>
            <TextBlock x:Name="DeviceIPText" Text="192.168.1.1" FontSize="12"
                       Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Form Content -->
        <StackPanel Grid.Row="1">
            <!-- Device Name -->
            <Label Content="Device Name:" Margin="0,0,0,5"/>
            <TextBox x:Name="DeviceNameTextBox" Padding="8" Margin="0,0,0,15"
                     Background="{DynamicResource PanelBackgroundBrush}"
                     BorderBrush="{DynamicResource PanelBorderBrush}"/>

            <!-- Notes -->
            <Label Content="Notes:" Margin="0,0,0,5"/>
            <TextBox x:Name="NotesTextBox" AcceptsReturn="True" TextWrapping="Wrap"
                     Height="150" Padding="8" VerticalScrollBarVisibility="Auto"
                     Background="{DynamicResource PanelBackgroundBrush}"
                     BorderBrush="{DynamicResource PanelBorderBrush}"/>
        </StackPanel>

        <!-- Buttons -->
        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" Content="Save" Width="80" Margin="0,0,10,0"
                    Click="Save_Click" IsDefault="True"
                    Background="{DynamicResource AccentBrush}" Foreground="White"
                    Padding="12,6" BorderThickness="0" Cursor="Hand"/>
            <Button Grid.Column="2" Content="Cancel" Width="80"
                    Click="Cancel_Click" IsCancel="True"
                    Padding="12,6" Cursor="Hand"/>
        </Grid>
    </Grid>
</Window>
